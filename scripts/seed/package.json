{
  "name": "@repo/seed",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun --env-file=../../.env --env-file=.env run --bun src/index.ts",
    "check-types": "tsc --noEmit",
    "lint": "eslint --cache"
  },
  "dependencies": {
    "@repo/auth": "workspace:*",
    "@repo/db": "workspace:*",
    "@t3-oss/env-core": "catalog:",
    "better-auth": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
